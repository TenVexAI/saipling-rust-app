%% ─────────────────────────────────────────────────────────────────────────────
%% sAIpling Export Template: Minimal (LaTeX / XeLaTeX)
%% Clean, simple layout using the `article` class.
%% Good for sharing drafts; no fancy chapter styles.
%% ─────────────────────────────────────────────────────────────────────────────
\documentclass[12pt, $if(papersize)$$papersize$$else$letterpaper$endif$]{article}

%% ── Page geometry ─────────────────────────────────────────────────────────
\usepackage[
  margin=1.25in,
  top=1.1in,
  bottom=1.1in,
]{geometry}

%% ── Fonts ─────────────────────────────────────────────────────────────────
\usepackage{fontspec}
\setmainfont[Ligatures=TeX]{$if(mainfont)$$mainfont$$else$Georgia$endif$}

%% ── Line spacing ──────────────────────────────────────────────────────────
\usepackage{setspace}
\setstretch{1.4}

%% ── Paragraph style ───────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

%% ── Section styles ────────────────────────────────────────────────────────
\usepackage{titlesec}

% Chapter-level (section in article) — left-aligned, light weight
\titleformat{\section}
  {\normalfont\fontsize{18}{22}\selectfont}
  {}
  {0em}
  {}
  [\vspace{0.05in}\titlerule[0.4pt]]

\titlespacing*{\section}{0pt}{2.5in}{0.5in}

% Scene break (subsection)
\titleformat{\subsection}
  {\normalfont\small\scshape\centering}
  {}
  {0em}
  {}

\titlespacing*{\subsection}{0pt}{0.3in}{0.25in}

% Sub-subsection
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {}
  {0em}
  {}

\titlespacing*{\subsubsection}{0pt}{0.2in}{0.1in}

%% ── Page style ────────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\small\thepage}

%% ── Pandoc compatibility ─────────────────────────────────────────────────
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage[english]{babel}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

$if(title)$
\begin{center}
  {\large $title$}\\[0.5em]
  $if(author)${\normalsize $author$}\\$endif$
  $if(date)${\small $date$}$endif$
\end{center}
\vspace{0.5in}
$endif$

$body$

\end{document}
